---
title: "GAM development"
format:
  html:
    code-fold: true
jupyter: python3
---

```{python}
import os
os.chdir("..")
```

```{python}
from src.bayesgam.bayesgam import GAM
from pygam import LinearGAM, s, f
from pygam.datasets import wage
import pandas as pd
import numpy as np
```


```{python}
# Example usage
X = np.array([0, 1, 2, 3])
Y = np.array([3, 2, 1, 0])

# Instantiate the model with a formula
model = GAM('f(X[0]) + f(X[1]) + f(X[3])')
model.fit(X,Y)
```


```{python}
X, y = wage()

```